# 示例 OpenAI 配置
# 请在本地创建 server/.env 并填写你的 OpenAI API Key
OPENAI_API_KEY=sk-REPLACE_ME
# 可选：覆盖 OpenAI API 的 base URL（例如指向自建的中继/代理）
# 可设置为：https://api.openai.com 或 http://localhost:8082 等
OPENAI_API_BASE=
# 可选：指定 AZURE API 版本（如果使用 Azure OpenAI）
AZURE_API_VERSION=
# Server 监听端口（默认 8000）
PORT=8000
